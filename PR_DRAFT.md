## Pull Request Draft: feat/kinvo-implementation

**Title:** feat: implement Kinvo Front-End challenge (React + styled-components)

**Branch:** `feat/kinvo-implementation` (pushed to fork: `git@github.com:giselleandrade1/kinvo-frontend.test.git`)

**Summary**
- Implemented a Vite + React app that fulfills the Kinvo Front-End challenge (Pleno level).
- Features: search (by name/type/institution), sort (selector), pagination (5 per page), API fetch from mocked endpoint, sparkline mini-chart for each item.
- Code organized in reusable components (`Header`, `Controls`, `ProductCard`, `Pagination`, `Sparkline`) and a service layer (`src/services/api.js`).
- Tests: Vitest + Testing Library unit tests covering fetch/render, search, and pagination. Tests run under jsdom and include a small `ResizeObserver` polyfill for Recharts.

**Files / notable changes**
- `src/App.jsx` — main composition, fetch + state management
- `src/components/*` — UI components (Header, Controls, ProductCard, Pagination, Sparkline)
- `src/services/api.js` — API wrapper `fetchFixedIncomeData()`
- `src/setupTests.js` — test setup (jest-dom matchers + ResizeObserver polyfill)
- `src/__tests__/App.test.jsx` — unit tests
- `README.md` — updated instructions and architecture notes

**Why this PR**
This PR completes an end-to-end implementation of the challenge including tests and CI configuration. It's ready for review and merge into the upstream repository.

**How to run locally**
1. Install dependencies:
```bash
npm install
```
2. Run dev server:
```bash
npm run dev
```
3. Run tests:
```bash
npm test -- --run
```

**Notes for reviewers**
- Tests pass locally (Vitest) — see `src/__tests__/App.test.jsx`.
- I added a small `ResizeObserver` polyfill in tests so Recharts' `ResponsiveContainer` won't error under jsdom; this is only for test environment.
- The sparkline uses `recharts` and a synthetic fallback series if the API item lacks historical data.

**PR checklist**
- [ ] Code builds and runs locally
- [ ] Tests pass
- [ ] Components are reusable and documented in README
- [ ] Linting & formatting as needed

**Suggested PR description (copy to GitHub when creating PR)**
This PR implements the Kinvo Front-End challenge (Pleno level) using React and styled-components. It adds search, sort, and pagination features, includes unit tests with Vitest, and integrates sparkline charts for items. All tests pass locally and the branch is ready for review.

**How to open the PR**
- Via GitHub web: open a PR from `giselleandrade1/kinvo-frontend.test:feat/kinvo-implementation` to the upstream repository (e.g., `kinvoapp/kinvo-front-end-test`).
- Via `gh` CLI (if installed & authenticated):
```bash
gh pr create --base main --head giselleandrade1:feat/kinvo-implementation --title "feat: implement Kinvo Front-End challenge" --body-file PR_DRAFT.md
```

If you'd like, I can try to open the PR automatically, but it requires GitHub credentials/token.

---
Generated by the local dev assistant to make the PR creation step quick. Let me know if you want me to open the PR or adjust the PR description.
